services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: malaga
      POSTGRES_USER: malaga
      POSTGRES_PASSWORD: malaga
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
